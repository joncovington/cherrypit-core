"""cherrypick.core — shared core for the Cherrypick trading suite.

A library, never a service, on any loop decision path (suite invariant). Sub-packages are seeded
incrementally per the roadmap; `auth` is first.
"""

__version__ = "0.1.0"
